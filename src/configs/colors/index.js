import {Colors} from 'react-native/Libraries/NewAppScreen';

const colorReferences = {
  baseWhite: '#FFFFFF',
  baseBlack: '#000000',
};

const colors = {
  ...colorReferences,
};

export default colors;
